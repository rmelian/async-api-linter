{
  "name": "async-api-linter",
  "version": "0.0.1",
  "description": "",
  "bin": {
    "async-api-linter": "./cli.js"
  },
  "scripts": {
    "test": "nyc --reporter=html --reporter=text mocha",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "preferGlobal": true,
  "keywords": [
    "asyncapi",
    "documentation",
    "validator",
    "validation",
    "resolver",
    "lint",
    "linter"
  ],
  "dependencies": {
    "commander": "^2.18.0",
    "js-yaml": "^3.6.1",
    "nconf-yaml": "^1.0.2",
    "z-schema": "^3.18.2",
    "asyncapi": "^2.0.2",
    "json-schema-ref-parser": "^5.1.0"
  },
  "devDependencies": {
    "babel-core": "^6.24.1",
    "babel-loader": "^8.0.2",
    "babel-preset-es2015": "^6.24.1"
  }
}
